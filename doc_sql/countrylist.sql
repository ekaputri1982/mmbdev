CREATE TABLE `countrylist` (
	`country_id` INT(3) NOT NULL AUTO_INCREMENT,
	`country_name` VARCHAR(100) NOT NULL,
	`country_abbrv` VARCHAR(2) NOT NULL,
	`curr_id` INT(5) NOT NULL,
	PRIMARY KEY (`country_id`)
)
AUTO_INCREMENT=7
;